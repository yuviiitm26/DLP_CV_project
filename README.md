# DLP_CV_project
A computer vision pipeline that transforms a single flat RGB image into a depth-ordered, 3D-aware layered representation. It extracts semantic objects, sorts them by depth, and inpaints occluded backgrounds for editing, relighting, and 2.5D parallax animation.
